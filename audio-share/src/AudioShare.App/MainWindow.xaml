<Window x:Class="AudioShare.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="FlowCast - 音樂分享"
        Icon="/Assets/flowcast-logo.ico"
        Width="1120"
        Height="740"
        MinWidth="980"
        MaxWidth="1280"
        MinHeight="680"
        MaxHeight="840"
        ResizeMode="CanResize"
        WindowStyle="None"
        Background="#F7F9FD"
        FontFamily="Microsoft YaHei UI"
        WindowStartupLocation="CenterScreen">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                           ResizeBorderThickness="6"
                           GlassFrameThickness="0"
                           CornerRadius="0"
                           UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="AppleCanvasBrush" Color="#F7F9FD" />
        <SolidColorBrush x:Key="AppleGlassBrush" Color="#D9FFFFFF" />
        <SolidColorBrush x:Key="AppleGlassBorderBrush" Color="#E8FFFFFF" />
        <SolidColorBrush x:Key="AppleTextBrush" Color="#152C45" />
        <SolidColorBrush x:Key="AppleSecondaryTextBrush" Color="#708197" />
        <SolidColorBrush x:Key="AppleBlueBrush" Color="#0A84FF" />
        <SolidColorBrush x:Key="AppleBluePressedBrush" Color="#0074E6" />
        <DropShadowEffect x:Key="SoftShadow" BlurRadius="24" ShadowDepth="8" Opacity="0.09" Color="#476481" />
        <DropShadowEffect x:Key="ButtonShadow" BlurRadius="16" ShadowDepth="5" Opacity="0.16" Color="#0A84FF" />

        <Style x:Key="AppleGlassCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource AppleGlassBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AppleGlassBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="22" />
            <Setter Property="Effect" Value="{StaticResource SoftShadow}" />
        </Style>
        <Style x:Key="GlassCard" TargetType="Border" BasedOn="{StaticResource AppleGlassCard}" />
        <Style x:Key="AppleUtilityButton" TargetType="Button">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Background" Value="#BFFFFFFF" />
            <Setter Property="BorderBrush" Value="#80FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{StaticResource AppleTextBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="16" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform />
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#F5FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.42" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.84" />
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ButtonBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.025" Duration="0:0:0.16" />
                                        <DoubleAnimation Storyboard.TargetName="ButtonBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.025" Duration="0:0:0.16" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ButtonBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.16" />
                                        <DoubleAnimation Storyboard.TargetName="ButtonBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.16" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="UtilityButton" TargetType="Button" BasedOn="{StaticResource AppleUtilityButton}" />
        <Style x:Key="ApplePrimaryButton" TargetType="Button" BasedOn="{StaticResource AppleUtilityButton}">
            <Setter Property="Padding" Value="21,11" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="{StaticResource AppleBlueBrush}" />
            <Setter Property="BorderBrush" Value="#5FB3FF" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Effect" Value="{StaticResource ButtonShadow}" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ApplePrimaryButton}" />
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E85558" />
            <Setter Property="BorderBrush" Value="#FF9697" />
        </Style>
        <Style x:Key="ProgramCheckBox" TargetType="CheckBox">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Box" Width="22" Height="22" Background="#EFFFFFFF" BorderBrush="#B7C7D9" BorderThickness="1.4" CornerRadius="11">
                            <Path x:Name="Check" Data="M 5,11 L 9,15 L 17,7" Stroke="White" StrokeThickness="2.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Visibility="Collapsed" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource AppleBlueBrush}" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AppleBlueBrush}" />
                                <Setter TargetName="Check" Property="StrokeThickness" Value="2.8" />
                                <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="1" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Box" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="9,6" />
            <Setter Property="Background" Value="#D8FFFFFF" />
            <Setter Property="BorderBrush" Value="#B6C7D9" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="KeyboardNavigation.IsTabStop" Value="False" />
            <Setter Property="MaxDropDownHeight" Value="288" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="Background" Value="#EFFFFFFF" />
            <Setter Property="BorderBrush" Value="#B9D0E5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{StaticResource AppleTextBrush}" />
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource AppleTextBrush}" />
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#D6EAFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="WindowCommandButton" TargetType="Button">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="#58708A" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CommandSurface" Margin="3" Background="{TemplateBinding Background}" CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CommandSurface" Property="Background" Value="#E2EEF8" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CommandSurface" Property="Background" Value="#D5E4F0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid ClipToBounds="True">
        <Rectangle Fill="{StaticResource AppleCanvasBrush}" />
        <Ellipse Width="420" Height="390" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-180,-120,0" Opacity="0.28">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#A3DBFF" Offset="0" />
                    <GradientStop Color="#00FFFFFF" Offset="1" />
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>
        <Ellipse Width="300" Height="270" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-120,0,0,-100" Opacity="0.22">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#FFB9A6" Offset="0" />
                    <GradientStop Color="#00FFFFFF" Offset="1" />
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <Grid x:Name="MainContent" Margin="30,62,30,28" Opacity="0" Visibility="Hidden">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="4,0,4,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Border x:Name="LogoMark" Width="52" Height="52" Margin="0,0,14,0" Background="#DCFFFFFF" BorderBrush="#EFFFFFFF" BorderThickness="1" CornerRadius="18" Effect="{StaticResource SoftShadow}">
                        <Image Margin="10" Source="/Assets/flowcast-logo.png" Stretch="Uniform" />
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock FontSize="26" FontWeight="SemiBold" Foreground="#172D45" Text="FlowCast" />
                        <TextBlock Margin="1,1,0,0" Foreground="#687D94" Text="音樂分享" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource UtilityButton}" Click="SettingsButton_Click" Content="設置" />
                    <Button Style="{StaticResource UtilityButton}" Click="TutorialButton_Click" Content="教程" />
                    <Button Style="{StaticResource UtilityButton}" Click="ShareHistoryButton_Click" Content="歷史" />
                    <Button x:Name="UpdateButton" Style="{StaticResource UtilityButton}" Visibility="Collapsed" Click="UpdateButton_Click" Content="更新" />
                    <Button x:Name="RefreshButton" Style="{StaticResource UtilityButton}" Margin="0" Click="RefreshButton_Click" Content="刷新" />
                </StackPanel>
            </Grid>

            <Border x:Name="TopStatusCard" Grid.Row="1" Style="{StaticResource GlassCard}" Padding="24" Margin="0,0,0,18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border x:Name="StatusPulse" Width="76" Height="76" Background="#E8FFFFFF" BorderBrush="#F3FFFFFF" BorderThickness="1" CornerRadius="38" VerticalAlignment="Center">
                        <Viewbox Margin="18">
                            <Path Data="M 4,12 C 4,7 8,4 12,4 C 16,4 20,7 20,12 M 8,12 C 8,9 10,8 12,8 C 14,8 16,9 16,12 M 2,10 L 2,14 M 22,10 L 22,14" Stroke="#12AFC0" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                        </Viewbox>
                    </Border>
                    <Grid Grid.Column="1" Margin="18,0,28,0" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="126" Height="28" IsHitTestVisible="False">
                        <Path x:Name="RouteFlowPath" VerticalAlignment="Center" Data="M 0,12 C 18,0 38,24 60,12 S 102,0 126,12" Stroke="#48CFC3" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.64" />
                        <Canvas Width="126" Height="28">
                            <Ellipse x:Name="RouteBeaconOne" Width="7" Height="7" Canvas.Left="0" Canvas.Top="9" Fill="#18AFC0" Opacity="0" />
                            <Ellipse x:Name="RouteBeaconTwo" Width="7" Height="7" Canvas.Left="0" Canvas.Top="9" Fill="#7ADCD4" Opacity="0" />
                        </Canvas>
                    </Grid>
                    <StackPanel Grid.Column="1" Margin="18,0,20,0" VerticalAlignment="Center">
                        <TextBlock x:Name="FlowCastStatusText" FontSize="27" FontWeight="SemiBold" Foreground="#117F95" Text="正在確認分享狀態" />
                        <TextBlock x:Name="FlowCastStatusHintText" Margin="0,5,0,0" FontSize="14" Foreground="#496C82" TextWrapping="Wrap" Text="正在檢查當前程序。" />
                        <TextBlock x:Name="B1StatusText" Margin="0,5,0,0" FontSize="13" FontWeight="SemiBold" Foreground="#278093" Text="B1：正在檢測" />
                        <StackPanel x:Name="B1MeterPanel" Margin="0,5,0,0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="100" Height="6" Background="#D6E8EE" CornerRadius="3" ClipToBounds="True">
                                <Border x:Name="B1MeterFill" Width="0" HorizontalAlignment="Left" Background="#1BBFAD" CornerRadius="3" />
                            </Border>
                            <TextBlock x:Name="B1MeterValueText" Margin="8,0,0,0" FontSize="12" Foreground="#70869A" Text="0%" />
                        </StackPanel>
                        <TextBlock x:Name="B1SelfTestText" Margin="0,6,0,0" FontSize="12" Foreground="#70869A" Text="B1 自測：開始分享後會在這裡確認音頻信號。" />
                        <TextBlock x:Name="TotalShareDurationText" Margin="0,6,0,0" FontSize="12" FontWeight="SemiBold" Foreground="#278093" Text="累計分享 00:00:00" />
                        <TextBlock x:Name="RecentActivityText" Margin="0,8,0,0" FontSize="12" Foreground="#70869A" TextWrapping="Wrap" />
                        <StackPanel x:Name="B1ActivityBars" Margin="0,13,0,0" Orientation="Horizontal" Opacity="0.65">
                            <Rectangle Width="4" Height="10" Margin="0,0,4,0" Fill="#36C8C2" RadiusX="2" RadiusY="2" />
                            <Rectangle Width="4" Height="20" Margin="0,0,4,0" Fill="#36C8C2" RadiusX="2" RadiusY="2" />
                            <Rectangle Width="4" Height="14" Margin="0,0,4,0" Fill="#36C8C2" RadiusX="2" RadiusY="2" />
                            <Rectangle Width="4" Height="27" Margin="0,0,4,0" Fill="#36C8C2" RadiusX="2" RadiusY="2" />
                            <Rectangle Width="4" Height="17" Margin="0,0,4,0" Fill="#36C8C2" RadiusX="2" RadiusY="2" />
                            <Rectangle Width="4" Height="10" Fill="#36C8C2" RadiusX="2" RadiusY="2" />
                        </StackPanel>
                        <StackPanel Margin="0,13,0,0" Orientation="Horizontal" Height="26" VerticalAlignment="Bottom">
                            <Border x:Name="AtmosphereBarOne" Width="6" Height="18" Margin="0,0,5,0" Background="#36C8C2" CornerRadius="3" RenderTransformOrigin="0.5,1" />
                            <Border x:Name="AtmosphereBarTwo" Width="6" Height="26" Margin="0,0,5,0" Background="#48CFC3" CornerRadius="3" RenderTransformOrigin="0.5,1" />
                            <Border x:Name="AtmosphereBarThree" Width="6" Height="15" Margin="0,0,5,0" Background="#36C8C2" CornerRadius="3" RenderTransformOrigin="0.5,1" />
                            <Border x:Name="AtmosphereBarFour" Width="6" Height="24" Margin="0,0,5,0" Background="#48CFC3" CornerRadius="3" RenderTransformOrigin="0.5,1" />
                            <Border x:Name="AtmosphereBarFive" Width="6" Height="20" Margin="0,0,5,0" Background="#36C8C2" CornerRadius="3" RenderTransformOrigin="0.5,1" />
                            <Border x:Name="AtmosphereBarSix" Width="6" Height="28" Margin="0,0,5,0" Background="#48CFC3" CornerRadius="3" RenderTransformOrigin="0.5,1" />
                            <Border x:Name="AtmosphereBarSeven" Width="6" Height="16" Margin="0,0,5,0" Background="#36C8C2" CornerRadius="3" RenderTransformOrigin="0.5,1" />
                            <Border x:Name="AtmosphereBarEight" Width="6" Height="22" Background="#48CFC3" CornerRadius="3" RenderTransformOrigin="0.5,1" />
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="RestoreRoutingButton" Style="{StaticResource DangerButton}" Margin="0" IsEnabled="False" Click="RestoreRoutingButton_Click" Content="停止分享" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border x:Name="ApplicationListPanel" Grid.Row="2" Style="{StaticResource GlassCard}" Padding="24" MinHeight="300">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock FontSize="19" FontWeight="SemiBold" Foreground="#203A56" Text="可分享的程序" />
                            <TextBlock Margin="0,4,0,0" Foreground="#74879A" Text="一次只分享一個程序。選擇另一個程序會先恢復上一個程序。" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Top">
                            <Button Style="{StaticResource UtilityButton}" Margin="0" Click="HealthSummaryButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse x:Name="HealthSummaryDot" Width="8" Height="8" Margin="0,0,7,0" VerticalAlignment="Center" Fill="#1DBE86" />
                                    <TextBlock x:Name="HealthSummaryText" Foreground="#29425D" Text="系統狀態" />
                                    <TextBlock x:Name="HealthSummaryChevron" Margin="8,0,0,0" Foreground="#61798F" Text="⌃" />
                                </StackPanel>
                            </Button>
                            <Border x:Name="HealthDetailsPanel" Margin="0,8,0,0" Padding="4" Background="#8FFFFFFF" BorderBrush="#C8DAE8" BorderThickness="1" CornerRadius="14" Visibility="Collapsed">
                        <ItemsControl ItemsSource="{Binding HealthItems}" VerticalAlignment="Center">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel HorizontalAlignment="Right" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border MaxWidth="210" Margin="6,4,0,4" Padding="10,7" Background="{Binding Background}" BorderBrush="{Binding BorderBrush}" BorderThickness="1" CornerRadius="14">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <Ellipse Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding Accent}" />
                                                <TextBlock FontWeight="SemiBold" Foreground="#29425D" Text="{Binding Title}" />
                                            </StackPanel>
                                            <TextBlock Margin="14,3,0,0" FontSize="11" Foreground="#61798F" TextWrapping="Wrap" Text="{Binding Message}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                            </Border>
                        </StackPanel>
                    </Grid>
                    <TextBlock x:Name="EmptyStateText" Grid.Row="1" Margin="0,18,0,2" Foreground="#72879A" Text="正在檢測音頻會話..." />
                    <ListBox x:Name="ApplicationList" Grid.Row="2" Margin="0,10,0,0" ItemsSource="{Binding Applications}" Focusable="False" FocusVisualStyle="{x:Null}" KeyboardNavigation.IsTabStop="False" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Focusable" Value="False" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,4" Padding="16,10" Background="#CFFFFFFF" BorderBrush="#D6E5F1" BorderThickness="1" CornerRadius="18">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0" Margin="0,0,12,0" Style="{StaticResource ProgramCheckBox}" IsChecked="{Binding IsSelected, Mode=TwoWay}" IsEnabled="{Binding CanRequestSelection}" Checked="ApplicationSelectionChanged" Unchecked="ApplicationSelectionChanged" />
                                        <StackPanel Grid.Column="1" HorizontalAlignment="Left" MaxWidth="430">
                                            <TextBlock FontSize="15" FontWeight="SemiBold" Foreground="#253F5B" Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Margin="0,3,0,0" FontSize="12" Foreground="#788A9C" Text="{Binding DetailText}" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#277E91" Text="{Binding RouteStatusText}" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Margin="14,0,0,0" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Grid Width="184" Margin="0,0,7,0">
                                                <ComboBox ItemsSource="{Binding RouteOptions}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedRoute, Mode=TwoWay}" IsEnabled="{Binding CanEditRoute}" SelectionChanged="ApplicationRouteSelectionChanged">
                                                    <ComboBox.Style>
                                                        <Style TargetType="ComboBox">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                                            <Setter Property="Focusable" Value="False" />
                                                            <Setter Property="KeyboardNavigation.IsTabStop" Value="False" />
                                                            <Setter Property="MaxDropDownHeight" Value="288" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSharing}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ComboBox.Style>
                                                </ComboBox>
                                                <Border Padding="11,7" Background="#E8F8F7" BorderBrush="#9BDBD6" BorderThickness="1" CornerRadius="14" Visibility="Collapsed">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSharing}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock HorizontalAlignment="Center" FontWeight="SemiBold" Foreground="#167C78" Text="{Binding RouteDisplayText}" />
                                                </Border>
                                            </Grid>
                                            <Button Padding="9,5" Margin="0,0,7,0" Style="{StaticResource UtilityButton}" Click="HideProgramButton_Click" Content="不再顯示" />
                                            <Button Padding="9,5" Margin="0,0,7,0" Style="{StaticResource UtilityButton}" Click="FavoriteButton_Click" Content="{Binding FavoriteButtonText}" />
                                            <Border Padding="10,5" Background="#E8F1FF" CornerRadius="14">
                                                <TextBlock Foreground="#3972BC" Text="{Binding SelectionStatus}" />
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <StackPanel Grid.Row="3" Margin="0,18,0,0">
                <Border x:Name="VoicemeeterRequirementPanel" Style="{StaticResource GlassCard}" Padding="15" Background="#EEFFF3F3" BorderBrush="#F0B0B0" Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" Foreground="#B6373C" Text="需要安裝 Voicemeeter Banana" />
                            <TextBlock Margin="0,3,0,0" Foreground="#8C575A" Text="安裝後重啟電腦，再點“刷新”。" />
                        </StackPanel>
                        <Button Grid.Column="1" Style="{StaticResource DangerButton}" Click="DownloadVoicemeeterButton_Click" Content="官方下載" />
                    </Grid>
                </Border>

                <Border Margin="0,10,0,0" Style="{StaticResource GlassCard}" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="ExperimentalRoutingStatusText" Foreground="#426985" TextWrapping="Wrap" Text="正在檢查音頻路由..." />
                            <TextBlock x:Name="InstructionText" Visibility="Collapsed" />
                        </StackPanel>
                        <Button x:Name="ApplyRoutingButton" Grid.Column="1" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" IsEnabled="False" Visibility="Collapsed" Click="ApplyRoutingButton_Click" Content="開始分享" />
                    </Grid>
                </Border>

                <Border x:Name="ErrorPanel" Margin="0,10,0,0" Style="{StaticResource GlassCard}" Padding="12" Background="#EEFFF1F1" BorderBrush="#EAA8A8" Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" Foreground="#A42D36" TextWrapping="Wrap" />
                </Border>
            </StackPanel>
        </Grid>

        <Grid Height="42" VerticalAlignment="Top" Background="#ECFFFFFF" Panel.ZIndex="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border Grid.ColumnSpan="2" BorderBrush="#C9DCE9" BorderThickness="0,0,0,1" />
            <Border Grid.Column="0" Background="Transparent" MouseLeftButtonDown="WindowHeader_MouseLeftButtonDown" />
            <StackPanel Grid.Column="0" Margin="14,0" HorizontalAlignment="Left" IsHitTestVisible="False" Orientation="Horizontal" VerticalAlignment="Center">
                <Image Width="20" Height="20" Source="/Assets/flowcast-logo.png" Stretch="Uniform" />
                <TextBlock Margin="8,0,0,0" FontSize="13" FontWeight="SemiBold" Foreground="#25415D" Text="FlowCast" />
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="8,0" HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource WindowCommandButton}" Click="MinimizeWindow_Click" ToolTip="最小化" Content="-" />
                <Button Style="{StaticResource WindowCommandButton}" Click="CloseWindow_Click" ToolTip="關閉" Content="&#215;" />
            </StackPanel>
        </Grid>

        <Border x:Name="LaunchOverlay" Panel.ZIndex="20" Opacity="1" IsHitTestVisible="True">
            <Border.Background>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="#FFFFFF" Offset="0" />
                    <GradientStop Color="#F4F8FD" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid x:Name="LaunchBrand" Width="520" Height="260" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                    <Canvas Width="460" Height="160" HorizontalAlignment="Center" VerticalAlignment="Top">
                        <!-- Flow wave: the hero stroke, drawn first left to right. -->
                        <Path x:Name="LaunchFlow" Data="M 34,96 C 74,52 106,52 138,96 C 168,132 198,132 228,96" Stroke="#25B9C4" StrokeThickness="9" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" StrokeDashCap="Round" />
                        <!-- Music note head pops where the flow arrives. -->
                        <Ellipse x:Name="LaunchNoteHead" Canvas.Left="248" Canvas.Top="96" Width="34" Height="25" Fill="#25B9C4" RenderTransformOrigin="0.5,0.5" />
                        <!-- Music note stem and flag draw on. -->
                        <Path x:Name="LaunchNote" Data="M 280,108 L 280,46 C 304,50 318,64 308,84" Stroke="#25B9C4" StrokeThickness="9" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" StrokeDashCap="Round" />
                        <!-- Broadcast arcs (cast) draw outward. -->
                        <Path x:Name="LaunchCastArcOne" Data="M 332,74 A 27,27 0 0 1 332,122" Stroke="#2B4A6F" StrokeThickness="8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeDashCap="Round" />
                        <Path x:Name="LaunchCastArcTwo" Data="M 352,60 A 42,42 0 0 1 352,136" Stroke="#2B4A6F" StrokeThickness="8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeDashCap="Round" />
                        <!-- Play triangle pops last to complete the cast. -->
                        <Path x:Name="LaunchPlay" Data="M 390,80 L 420,98 L 390,116 Z" Fill="#2B4A6F" RenderTransformOrigin="0.5,0.5" />
                    </Canvas>
                    <TextBlock x:Name="LaunchTitle" Margin="0,182,0,0" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="Segoe UI Variable Display" FontSize="42" FontWeight="SemiBold" Foreground="#163653" Text="FlowCast" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>

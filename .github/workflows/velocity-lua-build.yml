name: MCB-v5-probe

on:
  push:
    branches:
      - mcb-v5-stability-panel
  workflow_dispatch: {}

jobs:
  probe:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Probe workflow
        shell: pwsh
        run: Write-Host "MCB v5 workflow is valid"
